version: "3.9"

services:
  tagmanager:
    build: .
    container_name: tagmanager
    ports:
      - "8080:8080"
    environment:
      HTTP_ADDR: ":8080"
      SAYMON_CONFIG_PATH: "/etc/saymon/saymon-server.conf"
      TAGS_COLLECTION: "tags"
    volumes:
      - /etc/saymon/saymon-server.conf:/etc/saymon/saymon-server.conf:ro
    restart: unless-stopped
